name: Build SingBox 1.12.14 Final Victory
on: workflow_dispatch

jobs:
  build:
    runs-on: macos-14
    steps:
      # 1. 🟢 环境强制对位 (解决 FATAL 报错)
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'microsoft'
          java-version: '17'
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      # 2. 🟢 源码强制对位 (物理隔离存放)
      - name: Clone Clean Source
        run: |
          # 物理创建独立文件夹，防止文件夹嵌套干扰
          mkdir -p $GITHUB_WORKSPACE/ui-app
          mkdir -p $GITHUB_WORKSPACE/core-src
          git clone https://github.com/SagerNet/sing-box-for-apple.git $GITHUB_WORKSPACE/ui-app
          git clone --branch v1.12.14 --depth 1 https://github.com/SagerNet/sing-box.git $GITHUB_WORKSPACE/core-src/singbox

      # 3. 🔴 核心对冲：解决路径缺失与 Module 冲突
      - name: Build Core Framework
        run: |
          # 配置工具链
          go install github.com/sagernet/gomobile/cmd/gomobile@latest
          go install github.com/sagernet/gomobile/cmd/gobind@latest
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH
          $(go env GOPATH)/bin/gomobile init
          
          # 🔴 关键修复：进入包含 go.mod 的物理绝对根目录
          cd $GITHUB_WORKSPACE/core-src/singbox
          
          # 🔴 关键修复：直接运行 1.12.x 官方专用的 Apple 构建包
          go run ./cmd/apple framework -v
          
          # 搬运生成的零件到 UI 项目
          mkdir -p $GITHUB_WORKSPACE/ui-app/Library/sing-box
          cp -rf *.xcframework $GITHUB_WORKSPACE/ui-app/Library/sing-box/

      # 4. 🟢 最终 Xcode 合成 (不使用有问题的 make_ios.sh)
      - name: Xcode Synthesis
        run: |
          cd $GITHUB_WORKSPACE/ui-app
          # 彻底清除干扰项
          rm -rf Extension IntentsExtension WidgetExtension TVExtension
          # 强制构建 IPA
          xcodebuild -project sing-box.xcodeproj \
                     -scheme SFI \
                     -configuration Release \
                     -sdk iphoneos \
                     CODE_SIGNING_ALLOWED=NO \
                     CODE_SIGNING_REQUIRED=NO \
                     FRAMEWORK_SEARCH_PATHS="$(pwd)/Library/sing-box" \
                     clean archive -archivePath build/sing-box.xcarchive

      # 5. 🟢 打包产出
      - name: Export IPA
        run: |
          mkdir -p Payload
          APP_PATH=$(find $GITHUB_WORKSPACE/ui-app/build/sing-box.xcarchive -name "*.app" | head -n 1)
          cp -rp "$APP_PATH" Payload/
          zip -r sing-box-1.12.14-stable.ipa Payload/

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: SingBox-Result
          path: sing-box-1.12.14-stable.ipa
