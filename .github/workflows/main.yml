name: Build SingBox 1.12.14 FINAL-STABLE
on: workflow_dispatch

jobs:
  build:
    # 🔴 核心修复 1：使用 macOS 15 环境，支持最新工程格式
    runs-on: macos-15
    steps:
      # 1. 🟢 环境锁死 (OpenJDK 17 必选)
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'microsoft'
          java-version: '17'
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      # 2. 🟢 纯净下载：隔离 UI 与 1.12.14 核心
      - name: Clone Sources
        run: |
          git clone --recursive https://github.com/SagerNet/sing-box-for-apple.git ui-src
          git clone --branch v1.12.14 --depth 1 https://github.com/SagerNet/sing-box.git core-src

      # 3. 🔴 绝杀：手动磨制零件并“强行改名”对位
      - name: Build Core Framework
        run: |
          go install github.com/sagernet/gomobile/cmd/gomobile@latest
          go install github.com/sagernet/gomobile/cmd/gobind@latest
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH
          $(go env GOPATH)/bin/gomobile init
          
          # 进入内核源码根目录，直接调用官方 1.12.x 原生编译入口
          cd core-src
          go run ./cmd/apple framework -v
          
          # 🔴 关键修复：把 1.12.14 的旧零件名强行改成新版 UI 期待的 Libbox
          cp -rf *.xcframework ../ui-src/Libbox.xcframework
          # 同时保留一份在旧版路径下作为双重保险
          mkdir -p ../ui-src/Library/sing-box
          cp -rf *.xcframework ../ui-src/Library/sing-box/

      # 4. 🔴 暴力剥离：清除所有会导致 Exit code 65 的签名组件
      - name: Purge Disruptive Extensions
        run: |
          cd ui-src
          # 彻底移除所有在云端无法通过签名校验的非核心插件
          rm -rf Extension IntentsExtension WidgetExtension TVExtension FileProviderExtension Shared
          # 预解析 Swift 包依赖
          xcodebuild -resolvePackageDependencies

      # 5. 🟢 最终合成 IPA (只编译主 App 方案)
      - name: Xcode Synthesis
        run: |
          cd ui-src
          xcodebuild -project sing-box.xcodeproj \
                     -scheme SFI \
                     -configuration Release \
                     -sdk iphoneos \
                     CODE_SIGNING_ALLOWED=NO \
                     CODE_SIGNING_REQUIRED=NO \
                     CODE_SIGN_IDENTITY="" \
                     CODE_SIGN_ENTITLEMENTS="" \
                     GCC_PREPROCESSOR_DEFINITIONS='$GCC_PREPROCESSOR_DEFINITIONS SFI_MAIN_ONLY=1' \
                     clean archive -archivePath build/sing-box.xcarchive

      # 6. 🟢 产出成品
      - name: Export for TrollStore
        run: |
          mkdir -p Payload
          APP_PATH=$(find ui-src/build/sing-box.xcarchive -name "*.app" | head -n 1)
          cp -rp "$APP_PATH" Payload/
          zip -r sing-box-1.12.14-victory.ipa Payload/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SingBox-Result-1.12.14
          path: sing-box-1.12.14-victory.ipa
